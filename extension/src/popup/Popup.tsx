import React from "react";
import ReactDOM from "react-dom";
import "webextension-polyfill-ts";

ReactDOM.render(
  <React.StrictMode>
    <p> what should I add here </p>
  </React.StrictMode>,
  document.getElementById("root")
);
